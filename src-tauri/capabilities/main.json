{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The converter does not expose native commands to the webview.",
  "windows": ["main"],
  "permissions": []
}
